function stringCounter(sentence) {
    var reslt = sentence.split(" ");
    console.log (reslt.length);
    var length = sentence.length;
    console.log(length)
}

let sentence2 = "A sunset on Mars is blue";
stringCounter(sentence2);

